Spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/echec
    username: postgres
    password: demoPassword
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true


server:
  port: 8080